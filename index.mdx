---
title: "Better Auth Invite Plugin"
mode: "custom"
---

<div className="relative overflow-hidden bg-gradient-to-br from-[#1e3a8a] via-[#1e40af] to-[#3b82f6] dark:from-[#0f1729] dark:via-[#1e2a47] dark:to-[#1e3a8a] py-20">
  <div className="max-w-6xl mx-auto px-6 lg:px-8">
    <div className="grid lg:grid-cols-12 gap-12 items-center">
      <div className="lg:col-span-7">
        <h1 className="text-4xl sm:text-5xl lg:text-6xl font-bold text-white mb-6">
          Powerful invitation system for Better Auth
        </h1>
        <p className="text-lg sm:text-xl text-blue-100 dark:text-gray-300 max-w-2xl mb-8">
          Add a complete invitation system to your Better Auth application. Create, send, and manage invites for user sign-ups and role upgrades with built-in security and flexibility.
        </p>
        <div className="flex flex-wrap gap-4">
          <a
            href="/quickstart"
            className="inline-flex items-center px-6 py-3 rounded-lg bg-white dark:bg-white text-blue-600 dark:text-blue-600 font-semibold hover:bg-blue-50 dark:hover:bg-gray-100 transition-colors"
          >
            Get Started
          </a>
          <a
            href="/api/invite"
            className="inline-flex items-center px-6 py-3 rounded-lg border border-white/30 bg-white/10 text-white font-semibold hover:bg-white/20 transition-colors"
          >
            API Reference
          </a>
        </div>
      </div>
      <div className="lg:col-span-5 hidden lg:block">
        <div className="relative">
          <div className="absolute inset-0 bg-gradient-to-r from-blue-400/20 to-purple-400/20 blur-3xl rounded-full"></div>
          <div className="relative bg-[#1a1d27] dark:bg-[#0f1117] rounded-2xl p-8 border border-blue-400/20">
            <pre className="text-sm text-gray-300 overflow-x-auto">
              <code>{`import { invite } from 
  "better-auth-invite-plugin";

export const auth = betterAuth({
  plugins: [
    invite({
      async sendUserInvitation({
        email, role, url
      }) {
        await sendEmail(email, url);
      }
    })
  ]
});`}</code>
            </pre>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <h2 className="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white mb-4">
    Quick start
  </h2>
  <p className="text-base text-gray-600 dark:text-gray-400 mb-8">
    Get the invite plugin up and running in your Better Auth project in minutes.
  </p>

  <Steps>
    <Step title="Install the plugin">
      Add the Better Auth Invite Plugin to your project:

      <CodeGroup>
      ```bash npm
      npm install better-auth-invite-plugin
      ```
      ```bash pnpm
      pnpm add better-auth-invite-plugin
      ```
      ```bash yarn
      yarn add better-auth-invite-plugin
      ```
      ```bash bun
      bun add better-auth-invite-plugin
      ```
      </CodeGroup>

      <Note>Requires Better Auth v1.4.13 or newer.</Note>
    </Step>

    <Step title="Configure the server plugin">
      Import and add the invite plugin to your Better Auth configuration:

      ```ts
      import { betterAuth } from "better-auth";
      import { invite } from "better-auth-invite-plugin";

      export const auth = betterAuth({
        plugins: [
          invite({
            async sendUserInvitation({ email, role, url }) {
              // Send invitation email
              await sendInvitationEmail(email, url);
            }
          })
        ]
      });
      ```
    </Step>

    <Step title="Add the client plugin">
      Configure the client-side plugin to access invite methods:

      ```ts
      import { createClient } from "better-auth/client";
      import { inviteClient } from "better-auth-invite-plugin";

      const client = createClient({
        plugins: [inviteClient()]
      });
      ```
    </Step>

    <Step title="Create your first invite">
      Use the client to create and send invitations:

      ```ts
      const { data, error } = await client.invite.create({
        email: "user@example.com",
        role: "admin"
      });
      ```

      The invitee will receive an email with a link to activate their invitation.
    </Step>
  </Steps>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <h2 className="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white mb-4">
    Key features
  </h2>
  <p className="text-base text-gray-600 dark:text-gray-400 mb-8">
    Everything you need to build a complete invitation system.
  </p>

  <CardGroup cols={2}>
    <Card title="Email invitations" icon="envelope" href="/guides/email-integration">
      Send invitation emails with customizable handlers and templates.
    </Card>
    <Card title="Role-based access" icon="user-shield" href="/concepts/permissions">
      Automatically assign or upgrade user roles when invites are accepted.
    </Card>
    <Card title="Public & private invites" icon="key" href="/concepts/invitation-types">
      Create targeted invites for specific users or shareable codes for anyone.
    </Card>
    <Card title="Usage tracking" icon="chart-line" href="/guides/managing-invites">
      Track invitation usage and enforce maximum uses per invite.
    </Card>
    <Card title="Multiple token types" icon="fingerprint" href="/concepts/token-types">
      Support default tokens, short codes, or custom token generators.
    </Card>
    <Card title="Hooks & callbacks" icon="webhook" href="/guides/hooks-and-callbacks">
      Customize behavior at every step with comprehensive lifecycle hooks.
    </Card>
  </CardGroup>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <h2 className="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white mb-4">
    Explore the documentation
  </h2>
  <p className="text-base text-gray-600 dark:text-gray-400 mb-8">
    Learn how to integrate and customize the invite plugin for your use case.
  </p>

  <CardGroup cols={2}>
    <Card title="Core concepts" icon="book-open" href="/concepts/how-it-works">
      Understand how the invitation system works under the hood.
    </Card>
    <Card title="Setup guides" icon="wrench" href="/guides/server-setup">
      Step-by-step guides for server and client configuration.
    </Card>
    <Card title="API reference" icon="code" href="/api/invite">
      Complete API documentation for all plugin options and methods.
    </Card>
    <Card title="Examples" icon="lightbulb" href="/examples/simple-invitation">
      Real-world examples for common invitation workflows.
    </Card>
  </CardGroup>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <div className="bg-gradient-to-r from-blue-500/10 to-purple-500/10 dark:from-blue-500/5 dark:to-purple-500/5 rounded-2xl border border-blue-200 dark:border-blue-800/30 p-8 sm:p-12">
    <h2 className="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white mb-4">
      Ready to get started?
    </h2>
    <p className="text-base text-gray-600 dark:text-gray-400 mb-6 max-w-2xl">
      Follow our quickstart guide to add the invitation system to your Better Auth project in minutes.
    </p>
    <a
      href="/quickstart"
      className="inline-flex items-center px-6 py-3 rounded-lg bg-[#3B82F6] text-white font-semibold hover:bg-[#2563EB] transition-colors"
    >
      Get Started
    </a>
  </div>
</div>
